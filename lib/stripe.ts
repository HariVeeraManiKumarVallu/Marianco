// Stub: no Stripe in this project. Prevent build errors from imports.
export type Stripe = unknown;

export function getStripeServer(): null {
  return null;
}

const stripe = null;
export default stripe;